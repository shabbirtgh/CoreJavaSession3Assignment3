class example {
 int x; int y;
 String name;
 public static void main(String args[]) {
 example pnt = new example();
 System.out.println ("pnt is " + pnt.name + " " + pnt.x + " " + pnt.y);
} }

//Option d is correct

// After creating object memory is created for the instance members and they can be called by the object.memberName
//As the variable are not initialised so it will give the default values the the string i.e null and 0 for the integer